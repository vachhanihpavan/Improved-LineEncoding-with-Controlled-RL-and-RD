global max_rl rd_th max_rd s input_stream max_size
fprintf('Maximum Running Length = %d\n',max_rl);
fprintf('Maximum Running Disparity = %d\n',max_rd);
fprintf('Threshold Running Disparity = %d\n',rd_th);
fprintf('S block size = %d\n',s);
fprintf('Length of Stream = %d\n',max_size);
